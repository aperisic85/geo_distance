calculate distance between default and list of locations in CSV file. prints all spots outside given radius.
currently default location is hardcoded.
Usage: distane.exe --file <FILE> --radius <RADIUS>
